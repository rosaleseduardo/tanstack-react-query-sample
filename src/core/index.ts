import { TanStackProvider } from './core.component.TanStackProvider';
import { useQueryClient } from './core.component.TanStackProvider.utils';

export const Core = {
  Component: {
    TanStackProvider,
  },
  Util: {
    useQueryClient,
  },
};
